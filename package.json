{
  "name": "is-guid-cli",
  "version": "1.0.1",
  "description": "Check if the String is Guid in your terminal",
  "main": "./lib/index.js",
  "scripts": {
    "start": "nodemon src/index.ts",
    "create": "npm run build && npm run test",
    "build": "tsc -p .",
    "test": "npm i -g && is-guid-cli",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install"
  },
  "keywords": [
    "cli",
    "isguid",
    "Guid",
    "is-guid-cli",
    "package"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/saqibnoorani/is-guid-cli.git"
  },
  "author": "Najmus Saqib",
  "license": "MIT",
  "bin": {
    "is-guid-cli": "./lib/index.js"
  },
  "dependencies": {
    "chalk": "^2.4.1",
    "check-guid": "^1.0.2",
    "clear": "^0.1.0",
    "commander": "^2.19.0",
    "figlet": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^10.12.10",
    "nodemon": "^2.0.2",
    "ts-node": "^8.8.1",
    "tslint": "^6.1.0",
    "typescript": "^3.8.3"
  }
}
